name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  go:
    uses: myrgic/.github/.github/workflows/go-ci.yml@main
    with:
      go-version: '1.24'
  public-release-gate:
    uses: myrgic/.github/.github/workflows/public-release-gate.yml@main
